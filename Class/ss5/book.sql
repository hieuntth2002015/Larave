create table book(
                     author varchar(128),
                     title varchar (128),
                     type varchar (16),
                     year char(4),
                     isbn char (13) PRIMARY KEY
)